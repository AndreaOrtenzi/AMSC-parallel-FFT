#ifndef MCU_SIZE
#define MCU_SIZE 8 // power of 2
#endif
#ifndef NUM_CHANNELS
#define NUM_CHANNELS 1
#endif
#ifndef QUALITY // from 0 to 100 for function: int stbi_write_jpg(char const *filename, int w, int h, int comp, const void *data, int quality);
#define QUALITY 100
#endif 

using norm_type = float;
using phase_type = float; // can't be int it should allow type c = unsigned int a;